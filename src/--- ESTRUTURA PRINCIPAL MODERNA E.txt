// --- ESTRUTURA PRINCIPAL MODERNA E UNIFICADA ---
export default function App() {
  const [activeTab, setActiveTab] = useState('home');

  // Rola para o topo ao trocar de aba
  useEffect(() => {
    window.scrollTo(0, 0);
  }, [activeTab]);

  return (
    <div className="min-h-screen transition-colors duration-500" style={{ backgroundColor: colors.bgLight }}>
      
      {/* HEADER MODERNO */}
      <header className="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-slate-200 shadow-sm">
        <div className="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between">
          <div className="flex flex-col cursor-pointer" onClick={() => setActiveTab('home')}>
            <span className="font-serif text-3xl font-bold leading-none" style={{color: colors.primary}}>IPVP</span>
            <span className="text-[10px] uppercase font-bold tracking-tighter text-slate-500">Várzea do Poço-BA</span>
          </div>

          <div className="flex bg-slate-100 p-1.5 rounded-2xl gap-1">
            <button 
              onClick={() => setActiveTab('home')}
              className={cn(
                "px-6 py-2 rounded-xl text-sm font-bold transition-all duration-300",
                activeTab === 'home' ? "bg-white text-slate-900 shadow-sm" : "text-slate-500 hover:text-slate-700"
              )}
            >
              Portal
            </button>
            <button 
              onClick={() => setActiveTab('membros')}
              className={cn(
                "px-6 py-2 rounded-xl text-sm font-bold transition-all duration-300 flex items-center gap-2",
                activeTab === 'membros' 
                  ? "bg-amber-500 text-white shadow-lg shadow-amber-200" 
                  : "text-slate-500 hover:text-slate-700"
              )}
            >
              <ShieldCheck size={16} /> Área Restrita
            </button>
          </div>
        </div>
      </header>

      {/* CONTEÚDO DINÂMICO INTERATIVO */}
      <main className="animate-in fade-in zoom-in duration-700">
        {activeTab === 'home' ? (
          <div className="space-y-0">
            {/* Aqui estão TODOS os seus componentes originais preservados */}
            <Hero />
            <div className="max-w-7xl mx-auto -mt-20 relative z-10 px-6">
               <BibleQuickSearch />
            </div>
            <LyricsSearch />
            <AnnualAgenda />
            <Avisos />
            <Leadership />
            <SpiritualResources />
            <Birthdays />
            <YouTubeCourse />
            <Giving />
            <ContactSection />
          </div>
        ) : (
          <div className="max-w-7xl mx-auto py-12 px-6 min-h-[70vh]">
            {/* Componente administrativo com a senha 2933 */}
            <AdminMembers />
          </div>
        )}
      </main>

      {/* RODAPÉ ELEGANTE */}
      <footer className="py-16 text-center bg-white border-t border-slate-100">
        <div className="mb-6 flex justify-center">
          <div className="p-3 rounded-full bg-slate-50">
            <CrossIcon size={32} style={{color: colors.gold}} />
          </div>
        </div>
        <p className="font-serif font-bold text-2xl mb-2" style={{color: colors.primary}}>Igreja Presbiteriana de Várzea do Poço</p>
        <p className="text-xs font-semibold text-slate-400 tracking-widest uppercase mb-8">Fidelidade à Palavra de Deus e Compromisso com a Verdade</p>
        <div className="h-1 w-20 bg-amber-500 mx-auto rounded-full opacity-30" />
      </footer>

      <SpeedDialButton />
    </div>
  );
}